require('dotenv').config(); const mongoose = require('mongoose'); const User = require('./models/User'); mongoose.connect(process.env.MONGO_URI, { useNewUrlParser: true, useUnifiedTopology: true }).then(async () => { const user = await User.findOne().select('_id name email'); console.log('USER', user ? JSON.stringify(user.toObject()) : 'NONE'); await mongoose.disconnect(); }).catch(err => { console.error('ERR', err.message); process.exit(1); });
